---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: bazzite-gnome-rocm
# description will be included in the image's metadata
description: This is my personal OS image with kernel downgraded for ROCm support.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/bazzite-gnome
image-version: latest # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: /tmp/system  # Cambiato per evitare conflitti con il percorso esistente

  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/atim/starship/repo/fedora-%OS_VERSION%/atim-starship-fedora-%OS_VERSION%.repo
    install:
      - micro
      - starship
    remove: []

  - type: default-flatpaks
    notify: true # Send notification after install/uninstall is finished (true/false)
    system:
      install:
        - com.brave.Browser # Brave added
        - org.gnome.Loupe
      remove:
        - org.gnome.eog
    user: {} # Also add Flathub user repo, but no user packages

  - type: rpm-ostree
    install:
      - /tmp/system/usr/local/rpms/kernel-6.13.7-108.bazzite.fc42.x86_64.rpm
      - /tmp/system/usr/local/rpms/kernel-core-6.13.7-108.bazzite.fc42.x86_64.rpm
      - /tmp/system/usr/local/rpms/kernel-modules-6.13.7-108.bazzite.fc42.x86_64.rpm
      - /tmp/system/usr/local/rpms/kernel-modules-core-6.13.7-108.bazzite.fc42.x86_64.rpm
      - /tmp/system/usr/local/rpms/kernel-modules-extra-6.13.7-108.bazzite.fc42.x86_64.rpm
    remove:
      # Removed unnecessary modules for ROCm support
      - kmod-bmi260
      - kmod-gpd-fan
      - kmod-ayaneo-platform
      - kmod-openrazer
      - kmod-v4l2loopback
      - kmod-wl
      - kmod-kvmfr
      - kmod-nct6687d
      - kmod-ryzen-smu
      - kmod-vhba
      - kmod-xone
      - kmod-zenergy
